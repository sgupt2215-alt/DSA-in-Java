class Solution {
    public int reachNumber(int target) {
        target=Math.abs(target);
        int nummoves=0;
        int move=0;
        while(move < target || (move - target)%2!=0){
            nummoves++;
            move += nummoves;
        }
        return nummoves;
    }
    
    }
